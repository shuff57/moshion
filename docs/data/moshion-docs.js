window.MOSHION_DOCS = [{"slug":"overview","title":"Overview","pages":[{"title":"What is moSHion?","body":"moSHion is a beginner-friendly 2D game engine for the web. It pairs a simple drawing canvas with real Box2D physics (via planck.js), so you can draw shapes and make them bump into each other without wiring the two worlds together yourself.\n\nIt's aimed at people who've written a little JavaScript and want to make a game this afternoon — not a week from now. The whole API is a handful of globals: Sprite, Group, world, camera, kb, mouse.\n\nYou don't import anything. You don't install anything. You write two functions — setup and draw — and the engine runs them for you. Add an update function if you need per-frame input or logic.","code":"let player;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  player = new Sprite(200, 100, 40, 40);\n  player.color = 'deepskyblue';\n  new Sprite(200, 380, 400, 20, 'static');\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"The sketch lifecycle","body":"A moSHion sketch runs up to three functions on a loop. setup() runs once at the start — you make your canvas and your sprites there. update() runs every frame before physics steps — you read input and change velocities here. draw() runs every frame after physics — you clear the screen and draw any extras here.\n\nupdate() runs before physics on purpose. When you set player.vel.x in update(), the next physics step uses that new velocity. If you set it in draw(), physics has already moved the sprite for this frame.\n\nBy default the loop targets 60 frames per second. frameCount tells you how many frames have passed since the sketch started — handy for timing things without a clock.","code":"let player;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  player = new Sprite(200, 100, 30, 30);\n  new Sprite(200, 380, 400, 20, 'static');\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.vel.x = -4;\n  else if (kb.pressing('right')) player.vel.x = 4;\n}\n\nfunction draw() {\n  background('#222');\n  fill('white');\n  textSize(14);\n  text('frame: ' + frameCount, 10, 20);\n}"},{"title":"Global mode","body":"moSHion runs in \"global mode.\" That means Canvas, Sprite, world, kb, and friends are already on the page — you just use them. No import, no new Engine(), no setup boilerplate.\n\nThe same rule applies to setup, update, and draw. When you define a top-level function with one of those names, the engine finds it and calls it at the right time.\n\nTo share a sprite between setup and update, declare a let at the top of your file and assign to it inside setup. If you use const inside setup, the variable is trapped there and update can't see it.","code":"let player;\nlet ground;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  player = new Sprite(200, 100, 30, 30);\n  player.color = 'gold';\n  ground = new Sprite(200, 380, 400, 20, 'static');\n}\n\nfunction update() {\n  if (kb.presses(' ') && player.colliding(ground)) {\n    player.vel.y = -8;\n  }\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Debugging your sketch","body":"When something goes wrong, open your browser's developer console (F12 on most systems). Errors show up there with a line number. console.log(value) is your best friend — sprinkle it anywhere to see what's happening.\n\nThree mistakes trip up almost every beginner. First: forgetting background() in draw. The canvas never clears, so old frames pile up and everything looks smeared. Second: creating new Sprite() inside draw or update instead of setup. You'll spawn a new sprite every frame and the sketch will grind to a halt. Third: using a variable before it's assigned — usually because you wrote let player inside setup instead of at the top of the file.\n\nIf a sprite isn't showing up, log its x and y. If input doesn't work, log kb.pressing('left') inside update to confirm the key name.","code":"let player;\n\nfunction setup() {\n  new Canvas(400, 400);\n  player = new Sprite(200, 200, 30, 30);\n  console.log('setup ran, player at', player.x, player.y);\n}\n\nfunction update() {\n  if (kb.presses(' ')) {\n    console.log('space pressed on frame', frameCount);\n  }\n}\n\nfunction draw() {\n  background('#222');\n}"}]},{"slug":"canvas","title":"Canvas","pages":[{"title":"Creating the canvas","body":"new Canvas(width, height) inside setup() makes the drawing surface for your sketch. Without it, nothing shows up. Call it first, before you make any sprites, so the engine knows how big the world is.\n\nThe canvas coordinates start at (0, 0) in the top-left. X grows to the right, y grows downward. A 400 by 400 canvas has its center at (200, 200).\n\nThe default background is transparent — you'll usually replace it by calling background() inside draw.","code":"function setup() {\n  new Canvas(400, 400);\n}\n\nfunction draw() {\n  background('#202030');\n}"},{"title":"frameCount","body":"frameCount is a global number that starts at 0 and goes up by 1 every frame. It's the simplest way to animate: use it to offset a position, wrap a counter with %, or trigger something every N frames.\n\nCombine frameCount with Math.sin or Math.cos to get smooth back-and-forth motion without any physics.\n\nframeRate() (no arguments) returns how many frames per second your sketch is actually managing right now — call it inside draw() and print it if you want to see whether a busy scene is slowing things down.","code":"let ball;\n\nfunction setup() {\n  new Canvas(400, 400);\n  ball = new Sprite(200, 200, 40);\n  ball.collider = 'none';\n  ball.color = 'gold';\n}\n\nfunction update() {\n  ball.x = 200 + Math.sin(frameCount / 30) * 120;\n}\n\nfunction draw() {\n  background('#222');\n  fill('white');\n  textSize(14);\n  text('frame ' + frameCount, 10, 20);\n}"},{"title":"Background and clearing","body":"background(color) paints over the whole canvas. Call it first thing in draw() so last frame's pixels are erased before new ones go down. Skip it and you'll see comet-like trails behind every moving sprite.\n\nA solid color takes a name ('black', 'tomato'), a hex string ('#222', '#ff00aa'), or an rgba string. For a see-through background use 'rgba(0, 0, 0, 0.1)' — only 10% opaque, so old frames fade out gradually instead of vanishing.\n\nThat fade trick is how you get motion trails without tracking history yourself. The lower the alpha, the longer the trail.","code":"let p;\n\nfunction setup() {\n  new Canvas(400, 400);\n  p = new Sprite(200, 200, 20);\n  p.collider = 'none';\n  p.color = 'gold';\n}\n\nfunction update() {\n  p.x = 200 + Math.cos(frameCount / 20) * 150;\n  p.y = 200 + Math.sin(frameCount / 20) * 150;\n}\n\nfunction draw() {\n  background('rgba(0, 0, 0, 0.1)');\n}"}]},{"slug":"sprite","title":"Sprite","pages":[{"title":"Your first sprite","body":"A Sprite is the basic building block of any moSHion game. Every sprite has a position, a size, and (usually) a physics body that moves and collides automatically.\n\nnew Sprite(x, y, width, height) creates a rectangular sprite centered on (x, y). With no extra argument it's dynamic, meaning gravity pulls it and it bumps into other sprites.\n\nStore the sprite in a variable declared outside setup() so update() and draw() can reach it later. Without a reference, you can't move it or check its state.","code":"let player;\n\nfunction setup() {\n  new Canvas(400, 400);\n  player = new Sprite(200, 180, 40, 40);\n  player.color = 'deepskyblue';\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Collider types: dynamic, static, kinematic, none","body":"Every sprite has a physics type. You set it by passing a string as the last argument to new Sprite, or later with sprite.collider = '...'.\n\n'dynamic' is the default — gravity pulls it, other sprites push it. Use it for players, enemies, projectiles.\n\n'static' never moves and never reacts. Use it for floors, walls, and platforms. 'kinematic' moves only when you set its velocity — gravity ignores it. Use it for moving platforms and doors. 'none' turns physics off entirely — the sprite still draws, but nothing collides with it. Use it for cursors, UI, and decorations. A sprite with collider = 'none' is a sensor: you can still detect overlaps, but there's no physical pushback.","code":"let elevator;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n\n  new Sprite(200, 100, 40, 40);                    // dynamic (default)\n  new Sprite(200, 380, 400, 20, 'static');         // static floor\n  new Sprite(100, 260, 80, 10, 'static');          // static platform\n\n  elevator = new Sprite(320, 260, 60, 10, 'kinematic');\n  elevator.vel.y = -1;\n\n  const hint = new Sprite(200, 30, 10, 10);\n  hint.collider = 'none';\n  hint.color = 'gold';\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Position, rotation, scale","body":".x and .y are the center of the sprite, not the top-left corner. A sprite at (200, 200) has its middle at (200, 200).\n\n.rotation is in degrees. 90 is a quarter turn, 180 is a half turn, 360 is a full turn.\n\n.scale uniformly grows or shrinks how the sprite is drawn. The physics body stays the size you created it at, so very large scale values can look strange when the sprite collides.","code":"let box, spinner;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  box = new Sprite(120, 200, 40, 60);\n  box.rotation = 30;\n\n  spinner = new Sprite(280, 200, 40, 40);\n  spinner.collider = 'none';\n  spinner.color = 'gold';\n}\n\nfunction update() {\n  spinner.rotation += 3;\n  spinner.scale.x = 1 + 0.3 * Math.sin(frameCount / 20);\n  spinner.scale.y = spinner.scale.x;\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Color, visibility, and layer","body":".color sets the fill. You can use CSS names ('red'), hex ('#ff8800'), or rgb strings.\n\n.stroke is the outline color and .strokeWeight is its thickness in pixels. Set strokeWeight to 0 for no outline.\n\n.visible = false hides a sprite without removing it — physics still runs. .layer controls draw order — higher layer values draw on top.\n\n.opacity fades a sprite from 0 (invisible) to 1 (fully solid, the default) — good for a hit-flash, a fade-out before delete(), or a ghosted preview. .debug = true draws the sprite's actual physics collider as a lime outline, which is the fastest way to check whether the shape you're seeing matches the shape you're colliding with.","code":"function setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  const back = new Sprite(180, 200, 120, 120);\n  back.color = '#4455aa';\n  back.layer = 0;\n\n  const front = new Sprite(220, 200, 120, 120);\n  front.color = 'gold';\n  front.stroke = 'black';\n  front.strokeWeight = 3;\n  front.layer = 1;\n  front.opacity = 0.6;\n\n  const ball = new Sprite(340, 200, 40);\n  ball.color = 'tomato';\n  ball.debug = true; // outlines the real collider, useful while tuning shapes\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Shape options","body":"moSHion picks the collider shape from the constructor arguments. Two numbers make a 50×50 square: new Sprite(x, y). Three numbers make a circle: new Sprite(x, y, diameter). Four numbers make a rectangle: new Sprite(x, y, w, h).\n\nYou can also pass a body type as the last argument: new Sprite(x, y, w, h, 'static').\n\nThe engine chooses the collider shape from these arguments — you don't set a .shape property directly (though you can read it to check whether a sprite is 'circle' or 'rect').","code":"function setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n\n  new Sprite(90, 120, 40, 60);\n\n  const ball = new Sprite(200, 120, 40);\n  ball.color = 'tomato';\n\n  new Sprite(200, 380, 400, 20, 'static');\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Removing and cleaning up sprites","body":"sprite.delete() removes a sprite from the world. After deleting, it stops drawing, stops updating, and its physics body is gone. It also removes itself from every group it belongs to, including the implicit allSprites group. There is no undo — if you only want to hide a sprite, set .visible = false instead.\n\ngroup.remove(sprite) only unparents the sprite from that group — the sprite keeps existing, drawing, and running physics. These are NOT the same operation.\n\nWhen you delete sprites while looping through a Group, iterate a copy with [...group]. Otherwise the array shrinks under your feet and you'll skip items.","code":"let bullets;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  bullets = new Group();\n  bullets.color = 'gold';\n  bullets.diameter = 10;\n  bullets.collider = 'none';\n}\n\nfunction update() {\n  if (frameCount % 10 === 0) {\n    const b = new bullets.Sprite(20, 200);\n    b.vel.x = 4;\n  }\n  for (const b of [...bullets]) {\n    if (b.x > 400) b.delete();\n  }\n}\n\nfunction draw() {\n  background('#222');\n  fill('white');\n  textSize(14);\n  text('Live bullets: ' + bullets.length, 14, 24);\n}"},{"title":"Angular velocity","body":"sprite.angularVelocity is the rotation speed in degrees per frame. Positive spins clockwise, negative spins counter-clockwise.\n\nYou can read it to see how fast a sprite is spinning, or write it to give a sprite a spin kick — like a pendulum that gets pushed on a mouse click. The value is in degrees per frame, matching the facade's pixel-per-frame convention for linear velocity.","code":"let rod, anchor;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n\n  anchor = new Sprite(200, 80, 12, 12, 'static');\n  rod = new Sprite(200, 180, 12, 160);\n  rod.color = 'khaki';\n\n  new HingeJoint(anchor, rod);\n}\n\nfunction update() {\n  if (mouse.presses()) {\n    rod.angularVelocity = 6;\n  }\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Speed, direction, and moveTowards","body":".speed and .direction are another way to read and set .vel, in polar form instead of x/y. .speed is how fast the sprite is moving (pixels per frame, always 0 or positive — a negative speed just points direction the other way). .direction is the heading in degrees, where 0 points right, 90 points down, matching .rotation's convention.\n\nSetting .speed keeps the current direction; setting .direction keeps the current speed. That makes \"turn without slowing down\" or \"speed up without turning\" a one-line change instead of redoing the vector math.\n\nsprite.moveTowards(x, y, tracking) nudges a sprite toward a point without touching physics at all — each call moves it tracking (default 0.1, so 10%) of the remaining distance. Call it once per update() frame for a smooth chase that naturally slows down as it arrives.","code":"let hunter, target;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  hunter = new Sprite(60, 200, 30);\n  hunter.color = 'tomato';\n  hunter.collider = 'none';\n\n  target = new Sprite(340, 200, 16);\n  target.color = 'gold';\n  target.collider = 'none';\n}\n\nfunction update() {\n  target.x = 200 + Math.cos(frameCount / 40) * 150;\n  target.y = 200 + Math.sin(frameCount / 40) * 150;\n  hunter.moveTowards(target.x, target.y, 0.05);\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Chasing, fleeing, and pulling","body":"Two questions come first: how far away is it, and which way is it? sprite.distanceTo(other) gives the straight-line distance in pixels, and sprite.angleTo(other) gives the direction in degrees, measured the same way as sprite.rotation. Both take a pair of numbers or anything with x and y — another sprite, or mouse.\n\nmoveTowards has three relatives that work the same way, and all four accept either a pair of numbers or a target with .x and .y — so enemy.moveTowards(player, 0.02) is a complete chase.\n\nmoveTowards(target, tracking) closes that fraction of the remaining gap each call. moveAway(target, repel) is its mirror: it opens the gap by the same fraction. Both set position directly, so they ignore physics — the sprite arrives no matter what is in the way.\n\nattractTo(target, force) and repelFrom(target, force) work through physics instead, applying a real force in newtons toward or away from the point. The sprite accelerates, keeps its momentum, and bounces off whatever it hits on the way. Use these when the pull should feel like gravity or magnetism rather than a leash.\n\nrotateTowards(target, tracking) turns to face a point over several frames by setting a spin speed — unlike rotateTo, which steps or snaps the angle directly. Like rotateTo, it always turns the short way round.\n\nPassing mouse itself (not mouse.x and mouse.y) is worth doing: the sprite then ignores the cursor until it has actually moved over the canvas, instead of lunging at the top-left corner on the first frame.","code":"let hunter, prey, magnet;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  prey = new Sprite(200, 120, 18);\n  prey.color = 'gold';\n  prey.collider = 'none';\n\n  hunter = new Sprite(60, 120, 22);\n  hunter.color = 'tomato';\n  hunter.collider = 'none';\n\n  magnet = new Sprite(200, 300, 20);\n  magnet.color = 'deepskyblue';\n}\n\nfunction update() {\n  // prey runs from the cursor, hunter chases the prey\n  prey.moveAway(mouse, 0.04);\n  hunter.moveTowards(prey, 0.03);\n  hunter.rotateTowards(prey, 0.2);\n\n  // the blue one is pulled by physics, so it overshoots and swings back\n  magnet.attractTo(mouse, 30);\n}\n\nfunction draw() {\n  background('#222');\n  fill('white');\n  textSize(13);\n  text('move the mouse', 14, 22);\n}"},{"title":"rotateTo","body":"sprite.rotateTo(angle, speed) turns a sprite toward angle, in degrees, using the shorter direction. Pass speed to cap how many degrees it turns per call — call it every update() frame and it eases into the target angle instead of snapping. Leave speed out and it snaps immediately, same as setting .rotation directly.\n\nIt returns a Promise, so await sprite.rotateTo(90) works if you like that style — but the rotation itself already happened by the time you'd await it, so most lesson code just calls it and moves on.","code":"let turret;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  turret = new Sprite(200, 200, 60, 16);\n  turret.color = 'slategray';\n}\n\nfunction update() {\n  const dx = mouseX - turret.x, dy = mouseY - turret.y;\n  const angleToMouse = Math.atan2(dy, dx) * 180 / Math.PI;\n  turret.rotateTo(angleToMouse, 4); // turns at most 4°/frame toward the mouse\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Images and emoji on sprites","body":"sprite.image = 'player.png' loads an image from a URL and draws it in place of the default colored shape. The image stretches to fill the sprite's width and height.\n\nIf the string has no dot in it, moSHion treats it as an emoji: sprite.image = '🧍' draws that emoji character centered on the sprite at the sprite's height as the font size. This is a quick way to get a visible character without loading assets.\n\nSetting .image clears any active animation (sprite.ani), and vice versa — they're mutually exclusive.","code":"let player;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  player = new Sprite(200, 200, 64, 64);\n  player.collider = 'none';\n  player.image = '🦄';\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.x -= 3;\n  else if (kb.pressing('right')) player.x += 3;\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Textures: named pixel art","body":"sprite.texture = 'coin' picks art by NAME instead of by URL. moSHion ships a catalog of 40 pixel-art textures, so you can give a sprite a picture without finding, hosting, or spelling a file path.\n\nThe names, by group —\n\ncharacters: player, playerBeige, playerBlue, playerPink, playerYellow\n\npickups: coin, coinBronze, coinSilver, gem, gemGreen, gemOrange, gemYellow, heart, heartHalf, key, keyBlue, keyGreen, keyOrange, star\n\nblocks: block, bomb, cactus, crate, crateX, door, exitSign, mushroom, mushroomBrown, torch\n\ncreatures: bat, bee, bird, fish, fishPink, ghost, ladybug, slime, slimeBlue, snail, worm\n\nCall textureNames() to print the list from inside a sketch, and hasTexture('coin') to check one name. A name that does not exist warns in the console and suggests near matches rather than failing silently, so a typo tells you what it was.\n\nReading sprite.texture gives back the name you set, which makes if (s.texture === 'coin') work. Setting .texture clears .image and .ani, exactly the way those two already clear each other.\n\nTextures draw with smoothing turned off, so a 21x21 tile stretched across a 50x50 sprite stays blocky instead of blurring. A plain .image still smooths.\n\nDraw your own in the texture editor — the Textures tab in the sandbox, or the Textures link in the header. Open a built-in, change it, save it under any name. Save more than one frame and the texture ANIMATES: sprite.texture = 'myWalk' plays it, with no addAni call, because a saved animation is stored as the same horizontal frame strip addAni already slices.\n\nIn code, saveTexture('myGuy', dataUrl) stores a data: URL under a name, and from then on sprite.texture = 'myGuy' finds it — including in a later sketch, because it lives in the same store as storeItem(). A saved name beats a built-in one, so saving 'coin' replaces the built-in coin everywhere. deleteTexture('myGuy') forgets it. Keep saved art small: the store holds 64 KB per value and 512 KB in total, which is generous for pixel art and far too small for a photograph.\n\nThe built-in art is Kenney's \"Platformer Art: Pixel Redux\" pack, released under CC0 — public domain, free for classroom and commercial use alike.","code":"let player, coins;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  player = new Sprite(200, 300, 48, 48);\n  player.collider = 'none';\n  player.texture = 'player';\n\n  coins = new Group();\n  for (let i = 0; i < 5; i++) {\n    const c = new coins.Sprite(60 + i * 70, 120, 32, 32);\n    c.collider = 'none';\n    c.texture = 'coin';\n  }\n\n  console.log('textures available:', textureNames().length);\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.x -= 3;\n  else if (kb.pressing('right')) player.x += 3;\n\n  for (let i = coins.length - 1; i >= 0; i--) {\n    if (player.overlaps(coins[i])) coins[i].remove();\n  }\n}\n\nfunction draw() {\n  background('#222');\n}"}]},{"slug":"physics","title":"Physics","pages":[{"title":"Gravity and velocity","body":"world.gravity.y pulls every dynamic sprite downward. 10 feels roughly earth-like; set it to 0 for top-down games, or try negative values for \"upside-down\" worlds.\n\nEach sprite has a .vel vector with .x and .y components, measured in pixels per frame. At 60 fps, vel.x = 4 means the sprite moves 240 pixels per second. Reading .vel tells you how fast a sprite is moving; writing it overrides physics for that frame.\n\nAssigning vel every frame is fine for player controls — you're telling the engine exactly what you want.\n\nThere's a top speed. The physics engine won't let a sprite move faster than about 60 pixels per frame, no matter how high you set vel.x — it just quietly caps there. That's a safety limit so fast sprites can't tunnel through walls in a single frame. If a sprite feels stuck at the same speed no matter what number you try, this is why — it's not a bug.","code":"let player, ground;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  player = new Sprite(200, 100, 40, 40);\n  player.color = 'deepskyblue';\n  ground = new Sprite(200, 380, 400, 20, 'static');\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.vel.x = -5;\n  else if (kb.pressing('right')) player.vel.x = 5;\n  else                           player.vel.x = 0;\n\n  if (kb.presses(' ') && player.colliding(ground)) {\n    player.vel.y = -8;\n  }\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Bounciness and friction","body":".bounciness (also called restitution) is how much speed a sprite keeps after a collision. 0 means it sticks, 1 means it bounces back at full speed, values above 1 add energy (fun, but unstable).\n\n.friction resists sliding along a surface. 0 is ice — sprites slide forever. Higher values drag motion to a stop quickly.\n\nThese are per-sprite properties. When two sprites touch, Box2D combines their values — bounciness uses the higher of the two, friction uses a geometric mean.","code":"function setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n\n  const floor = new Sprite(200, 380, 400, 20, 'static');\n  floor.friction = 0.3;\n\n  const bouncy = new Sprite(120, 80, 30);\n  bouncy.bounciness = 0.9;\n  bouncy.color = 'tomato';\n\n  const dead = new Sprite(200, 80, 30);\n  dead.bounciness = 0;\n  dead.color = 'slategray';\n\n  const slippery = new Sprite(280, 80, 30);\n  slippery.friction = 0;\n  slippery.bounciness = 0.4;\n  slippery.color = 'deepskyblue';\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Mass and density","body":".mass is how heavy a sprite is — read it to see how physics will treat the sprite (a heavier sprite reacts less to the same applyForce push, see below). Setting .mass adjusts the sprite's density so its actual mass matches what you asked for. Static and kinematic sprites always have 0 mass, since gravity and pushes don't affect them.\n\n.density is the more direct knob underneath .mass — mass is density times the sprite's area, so a bigger sprite at the same density weighs more. New sprites default to a density of 1.\n\nYou'll reach for .mass far more often than .density — it's the number that actually means something (\"this rock should be twice as heavy as that crate\").","code":"function setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n\n  new Sprite(200, 380, 400, 20, 'static');\n\n  const feather = new Sprite(120, 80, 30);\n  feather.color = 'lightyellow';\n  feather.mass = 0.2;\n\n  const rock = new Sprite(280, 80, 30);\n  rock.color = 'gray';\n  rock.mass = 5;\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Applying forces","body":"sprite.applyForce(fx, fy) pushes a sprite with a force in newtons — the physics engine's own force unit, not pixels. Pass the x and y components as two separate numbers. Call it every frame for continuous thrust, or once for a shove.\n\nForces respect mass — heavier sprites react less to the same push than light ones do. This is different from setting .vel directly, which overrides physics entirely.\n\napplyForce is good for rockets, wind, magnets, or any continuous push where you want the physics engine to handle the acceleration naturally.","code":"let rocket;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  new Sprite(200, 380, 400, 20, 'static');\n\n  rocket = new Sprite(200, 300, 20, 60);\n  rocket.color = 'tomato';\n}\n\nfunction update() {\n  if (kb.pressing('up'))    rocket.applyForce(0, -40);\n  if (kb.pressing('left'))  rocket.applyForce(-20, 0);\n  if (kb.pressing('right')) rocket.applyForce(20, 0);\n\n  if (kb.presses(' ')) rocket.applyForce(0, -200);\n}\n\nfunction draw() {\n  background('#222');\n}"}]},{"slug":"collisions","title":"Collisions & Overlaps","pages":[{"title":"colliding vs overlapping","body":"There are two ways sprites can interact. Colliding is physical contact — the sprites push each other and bounce. Overlapping is just bounding-box intersection — the sprites pass through each other but you can still detect the touch.\n\nDynamic and static sprites collide by default. To let one sprite pass through another without stopping, set its .collider to 'none' — the sprite still draws and you can still test for overlap, but there's no physical pushback.\n\nUse colliding for ground checks, walls, and enemies that block the player. Use overlapping for coins, goal zones, power-ups, and other triggers that shouldn't shove the player.","code":"let player, wall, zone;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  player = new Sprite(80, 200, 30, 30);\n  player.color = 'deepskyblue';\n\n  wall = new Sprite(220, 200, 20, 200, 'static');\n  wall.color = 'slategray';\n\n  zone = new Sprite(340, 200, 60, 60);\n  zone.collider = 'none';\n  zone.color = 'gold';\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.vel.x = -4;\n  else if (kb.pressing('right')) player.vel.x = 4;\n  else                           player.vel.x = 0;\n\n  if (player.overlapping(zone)) zone.color = 'lime';\n  else                          zone.color = 'gold';\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Overlap callbacks","body":"sprite.overlaps(other) returns true if the two sprites intersect (bounding-box math for rectangles, real circle-distance math for two circles). Pass a Group and it returns true if any member overlaps.\n\nYou can also pass a callback as the second argument: sprite.overlaps(group, (self, other) => { ... }). The callback fires once per overlapping pair, after the method has finished its own iteration — so it's safe to delete() sprites inside the callback.\n\nThis is the cleanest way to collect pickups: player.overlaps(coins, (p, coin) => { score++; coin.delete(); }).","code":"let player, coins;\nlet score = 0;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  player = new Sprite(200, 200, 30, 30);\n  player.color = 'deepskyblue';\n\n  coins = new Group();\n  coins.diameter = 18;\n  coins.color = 'gold';\n  coins.collider = 'none';\n  new coins.Sprite(120, 120);\n  new coins.Sprite(300, 260);\n  new coins.Sprite(260, 320);\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.vel.x = -4;\n  else if (kb.pressing('right')) player.vel.x = 4;\n  else                           player.vel.x = 0;\n  if (kb.pressing('up'))         player.vel.y = -4;\n  else if (kb.pressing('down'))  player.vel.y = 4;\n  else                           player.vel.y = 0;\n\n  player.overlaps(coins, (p, coin) => {\n    score++;\n    coin.delete();\n  });\n}\n\nfunction draw() {\n  background('#222');\n  fill('white');\n  textSize(16);\n  text('Score: ' + score, 10, 24);\n}"},{"title":"Collisions with groups","body":"The collision functions work against a single sprite or a whole Group. player.colliding(enemies) returns a truthy number (a running frame count) while the player touches any sprite in the enemies group.\n\ncolliding() reads real physics contacts from Box2D — it's not the same as the manual bounding-box math that overlaps() uses. Use colliding(ground) before a jump to confirm the player is standing on something solid.\n\nIf you need to know which sprite was hit, iterate the group yourself and test one-by-one. When you delete sprites in response to a hit, loop over [...group] (a copy) so the shrinking group doesn't skip items.","code":"let player, enemies;\nlet hp = 3;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  player = new Sprite(60, 200, 30, 30);\n  player.color = 'deepskyblue';\n\n  enemies = new Group();\n  enemies.diameter = 24;\n  enemies.color = 'tomato';\n  enemies.collider = 'none';\n  new enemies.Sprite(220, 140);\n  new enemies.Sprite(260, 220);\n  new enemies.Sprite(320, 300);\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.vel.x = -3;\n  else if (kb.pressing('right')) player.vel.x = 3;\n  if (kb.pressing('up'))         player.vel.y = -3;\n  else if (kb.pressing('down'))  player.vel.y = 3;\n\n  for (const e of [...enemies]) {\n    if (player.overlapping(e)) {\n      hp--;\n      e.delete();\n    }\n  }\n}\n\nfunction draw() {\n  background('#222');\n  fill('white');\n  textSize(16);\n  text('HP: ' + hp, 14, 24);\n}"}]},{"slug":"groups","title":"Groups","pages":[{"title":"Spawning and defaults","body":"A Group holds many sprites that share traits. Set a property on the group and every sprite spawned into it inherits that value as its default.\n\nCreate a group with new Group(). Then spawn sprites into it with new groupName.Sprite(...) — note the capital S. That factory reads the group's color, diameter, collider, and other defaults, copies them onto the new sprite, and adds the sprite to the group.\n\nCommon defaults to set right after creating a group: color, diameter (for circles) or w/h, collider ('none' is useful for pickups and decorations), and layer.","code":"let stars;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  stars = new Group();\n  stars.color = 'gold';\n  stars.diameter = 16;\n  stars.collider = 'none';\n\n  new stars.Sprite(100, 100);\n  new stars.Sprite(200, 200);\n  new stars.Sprite(300, 300);\n}\n\nfunction draw() {\n  background('#112');\n}"},{"title":"Iterating and removing","body":"A Group extends Array. You get .length, indexing with group[i], for..of loops, and array methods like .forEach directly on the group.\n\nOne trap: removing sprites while iterating shifts the array and skips items. Iterate over a copy when you might remove entries: for (const s of [...group]). The spread makes a snapshot so the original can shrink safely.\n\ngroup.remove(sprite) pulls the sprite out of the group but leaves it in the world — it still draws and runs physics. sprite.delete() destroys the sprite entirely and unparents it from every group. These are different operations with different effects.","code":"let drops;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  drops = new Group();\n  drops.color = 'deepskyblue';\n  drops.diameter = 14;\n  drops.collider = 'none';\n}\n\nfunction update() {\n  if (frameCount % 10 === 0) {\n    new drops.Sprite(Math.random() * 400, 0);\n  }\n  for (const d of [...drops]) {\n    d.y += 3;\n    if (d.y > 420) d.delete();\n  }\n}\n\nfunction draw() {\n  background('#112');\n  fill('white');\n  textSize(14);\n  text('drops: ' + drops.length, 14, 22);\n}"},{"title":"Culling sprites that leave the screen","body":"Every spawner has the same problem: sprites that drift off the edge are invisible but still alive, still costing physics every frame. Left alone, a game that fires a bullet every 10 frames is dragging thousands of dead bullets around after a few minutes.\n\ngroup.cull(margin) deletes the members that have gone that far outside the canvas, and returns how many it deleted. cull(0) means \"the moment they leave\", cull(50) gives them a 50-pixel grace border so a sprite that is only halfway off doesn't vanish mid-view.\n\nGive it a function and it runs that instead of deleting: cull(0, s => s.y = 0). That's how you wrap a sprite around to the other side, or send it back to the spawn point, rather than destroying it. The sprite stays in the group either way.\n\nIt measures against the camera, so a scrolling game culls against what's actually on screen, not against the origin.","code":"let drops;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  drops = new Group();\n  drops.color = 'deepskyblue';\n  drops.diameter = 14;\n  drops.collider = 'none';\n}\n\nfunction update() {\n  if (frameCount % 10 === 0) {\n    new drops.Sprite(Math.random() * 400, 0);\n  }\n  for (const d of drops) d.y += 3;\n\n  // one line replaces the remove-while-iterating loop\n  drops.cull(20);\n}\n\nfunction draw() {\n  background('#112');\n  fill('white');\n  textSize(14);\n  text('alive: ' + drops.length, 14, 22);\n}"},{"title":"Sound","body":"loadSound('file.mp3') gives you a Sound. Call .play() for a one-off effect and .loop() for music that should keep going.\n\n.play() starts a fresh copy every time it's called, so firing a laser three times in three frames gives you three overlapping shots rather than one restarting over and over. .loop() is the opposite — it's the single track you start once and stop later, and calling it again while it's already going does nothing.\n\n.volume runs 0 to 1. .pan runs -1 (hard left) to 1 (hard right), so a sound can come from where the thing is: enemy.x < player.x ? -0.6 : 0.6. .rate is playback speed, and because it shifts the pitch too it's the cheapest way to get variety out of one sound: coin.rate = 1 + Math.random() * 0.3 makes every pickup sound slightly different. masterVolume(0.5) halves everything at once without touching any individual .volume — that's your mute button.\n\n.fade(to, seconds) ramps the volume instead of jumping it. music.fade(0, 2) is a two-second fade out, and it stops the sound when it lands so nothing keeps running silently.\n\nIf you have a lot of short effects, put them in one file and describe where each one lives: loadSound('sfx.mp3', { clips: { jump: [0, 0.25], hit: [0.5, 0.9] } }), then sfx.play('jump'). One download instead of a dozen. A clip name you haven't defined plays nothing, rather than blasting the whole file.\n\nTwo things that will confuse you the first time. Sound files have to download before they can play, so a .play() in the first moment of a sketch is silently ignored — that's why you load in setup() and play on an event. .loop() is the exception: it remembers, and starts as soon as the file arrives, because music.loop() in setup() would otherwise never play at all.\n\nAnd browsers refuse to make any noise until the player has clicked or pressed a key. moSHion unlocks the audio automatically on the first input, so mostly you won't notice — but it does mean a sound cannot play before the player has touched anything.","code":"let coin, music, player, coins;\n\nfunction setup() {\n  new Canvas(400, 300);\n  world.gravity.y = 0;\n\n  coin = loadSound('coin.mp3');\n  music = loadSound('music.mp3');\n  music.volume = 0.3;\n\n  player = new Sprite(60, 150, 24, 24);\n  player.color = 'deepskyblue';\n  player.collider = 'none';\n\n  coins = new Group();\n  coins.diameter = 14;\n  coins.color = 'gold';\n  coins.collider = 'none';\n  for (let i = 0; i < 4; i++) new coins.Sprite(140 + i * 60, 150);\n}\n\nfunction update() {\n  player.moveTowards(mouse, 0.08);\n\n  coins.overlaps(player, (c) => {\n    coin.rate = 1 + Math.random() * 0.3;   // a slightly different chime each time\n    coin.pan = (c.x - 200) / 200;          // and it comes from where the coin was\n    coin.play();\n    c.delete();\n  });\n\n  // safe to call every frame — loop() only ever starts one voice\n  music.loop();\n\n  // fade the music down as the coins run out\n  if (coins.length === 1 && music.volume > 0.1) music.fade(0.05, 1.5);\n}\n\nfunction draw() {\n  background('#111');\n  fill('white');\n  textSize(13);\n  text('click to start the music, move to collect', 14, 24);\n}"},{"title":"The allSprites group","body":"allSprites is a built-in Group that every sprite joins automatically the moment it's created. You never construct it — it's already there as soon as moSHion loads.\n\nIt's useful whenever you want to touch \"every sprite in the scene\" at once: allSprites.length for a live count, or for (const s of allSprites) to iterate the whole world.\n\nSince it extends Array like any other Group, the same patterns apply.","code":"let player, rocks;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  player = new Sprite(200, 200, 20);\n  player.color = 'white';\n\n  rocks = new Group();\n  rocks.color = 'slategray';\n  rocks.diameter = 18;\n  rocks.collider = 'none';\n  for (let i = 0; i < 5; i++) {\n    new rocks.Sprite(60 + i * 60, 320);\n  }\n}\n\nfunction draw() {\n  background('#112');\n  fill('white');\n  textSize(14);\n  text('allSprites: ' + allSprites.length, 14, 22);\n}"},{"title":"Custom properties","body":"You aren't limited to moSHion's built-in properties. Assign any property you like on a group and it becomes a default for every sprite spawned afterward — enemies.hp = 3 gives every new enemy an hp of 3.\n\nEach sprite gets its own copy of the value on spawn, so changing one enemy's hp doesn't affect the others. The group just holds the template.\n\nThis is how you keep gameplay data attached to the right sprite. Tag sprites with custom fields like .hp, .score, .kind, or anything else your game needs, and read them back off the individual sprite during collisions.","code":"let player, enemies;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  player = new Sprite(200, 200, 20);\n  player.color = 'white';\n\n  enemies = new Group();\n  enemies.color = 'tomato';\n  enemies.diameter = 20;\n  enemies.hp = 3;\n\n  for (let i = 0; i < 4; i++) {\n    new enemies.Sprite(80 + i * 70, 80);\n  }\n}\n\nfunction update() {\n  if (player.overlaps(enemies)) {\n    for (const e of enemies) {\n      if (player.overlaps(e)) { e.hp -= 1; if (e.hp <= 0) e.delete(); }\n    }\n  }\n}\n\nfunction draw() {\n  background('#112');\n}"}]},{"slug":"camera","title":"Camera","pages":[{"title":"Following a target","body":"The camera controls which part of the world is visible. camera.x and camera.y are the viewport's center in world coordinates.\n\nThe simplest follow is to snap the camera to the player each frame: camera.x = player.x. It works, but the view jitters on every tiny movement.\n\nA soft follow eases the camera toward the player with a catch-up factor: camera.x += (player.x - camera.x) * 0.1. Smaller factors feel floaty; larger factors feel snappy. 0.1 is a good starting point.","code":"let player;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  new Sprite(1000, 390, 2000, 20, 'static');\n  for (let x = 200; x < 2000; x += 300) {\n    new Sprite(x, 300, 60, 10, 'static');\n  }\n  player = new Sprite(200, 300, 30, 30);\n  player.color = 'deepskyblue';\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.vel.x = -5;\n  else if (kb.pressing('right')) player.vel.x = 5;\n  if (kb.presses(' ')) player.vel.y = -8;\n\n  camera.x += (player.x - camera.x) * 0.1;\n  camera.y += (player.y - camera.y) * 0.1;\n}\n\nfunction draw() {\n  background('#222');\n}"}]},{"slug":"input","title":"Input","pages":[{"title":"Keyboard basics","body":"The global kb object reports keyboard state every frame.\n\nkb.pressing('key') returns true while the key is held down. Use it for continuous actions like walking.\n\nkb.presses('key') is true only on the first frame of a press (edge-triggered). Use it for single-shot actions like jumping or firing.\n\nKey names are lowercase strings: 'left', 'right', 'up', 'down', 'a', 'w', 'enter', 'shift', 'escape', and either 'space' or ' ' for the space bar. Spelling is forgiving — 'space', 'ArrowRight' and 'arrowright' all find the key they name.\n\n'left', 'right', 'up' and 'down' answer for the arrow key OR its WASD twin, so kb.pressing('left') is true whether the player pressed the left arrow or 'a'. You get both control schemes without writing either one twice.\n\nThere are shorthand properties too: kb.space, kb.enter, kb.shift, kb.arrowUp and friends give the same number kb.pressing() would.\n\nArrow keys and space bar are automatically prevented from scrolling the page.","code":"let player;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  player = new Sprite(200, 200, 40, 40);\n  player.color = 'deepskyblue';\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.vel.x = -4;\n  else if (kb.pressing('right')) player.vel.x = 4;\n  else                           player.vel.x = 0;\n\n  if (kb.presses(' '))   player.color = 'gold';\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Holding a key","body":"kb.pressing('key') isn't just true/false — it's the number of frames the key has been held (0 when idle), so it's still truthy in an if-check but also useful as a charge-up timer.\n\nkb.holds('key') fires exactly once, the frame the hold crosses kb.holdThreshold (12 frames by default — about a fifth of a second at 60fps). kb.holding('key') is the frame count while held past that threshold (0 before it). kb.held('key') fires exactly once, the frame the key comes back up after having been held past the threshold — use it to tell a quick tap apart from a held press.\n\nmouse has the same four: mouse.holds(), mouse.holding(), mouse.held(), mouse.holdThreshold.","code":"let charge = 0;\n\nfunction setup() {\n  new Canvas(400, 200);\n}\n\nfunction update() {\n  if (kb.holds(' ')) charge = 0;\n  if (kb.holding(' ')) charge = kb.holding(' ');\n  if (kb.held(' ')) console.log('released a full charge:', charge);\n}\n\nfunction draw() {\n  background('#222');\n  fill('gold');\n  textSize(16);\n  text('hold space — charge: ' + charge, 14, 24);\n}"},{"title":"Mouse position and buttons","body":"mouse.x and mouse.y are the cursor's position in world coordinates (already camera-aware).\n\nmouse.pressing() returns true while any mouse button is held. mouse.presses() is true only on the first frame of a click — use it for single-shot actions. mouse.released() is true on the frame the button is released.\n\nTo tell the buttons apart, mouse.left, mouse.center and mouse.right each count one button on their own. mouse.pos is the cursor as an {x, y} object, handy for passing straight to a method that takes a target.\n\nmouse.x and mouse.y are world coordinates — they already include the camera, so comparing them to a sprite's x and y always works. mouse.canvasPos is the same cursor measured in screen pixels instead. Use it for anything pinned to the screen rather than the world, like a HUD button: once the camera scrolls, mouse.x is somewhere out in the level while canvasPos is still where the cursor looks like it is.\n\nCombine mouse position with world.getSpriteAt(x, y) to build click-to-select or drag interactions.","code":"let dot;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  dot = new Sprite(200, 200, 24);\n  dot.color = 'white';\n  dot.collider = 'none';\n}\n\nfunction update() {\n  dot.x = mouse.x;\n  dot.y = mouse.y;\n\n  if (mouse.pressing())       dot.color = 'gold';\n  else                        dot.color = 'white';\n}\n\nfunction draw() {\n  background('#222');\n  fill('white');\n  textSize(14);\n  text('x: ' + Math.round(mouse.x) + '  y: ' + Math.round(mouse.y), 14, 22);\n}"},{"title":"Hit-testing with the mouse","body":"world.getSpriteAt(x, y) returns the top-most (highest layer) sprite at that world position, or undefined if nothing is there. It checks both circles (radius test) and rectangles (bounding box).\n\nThis is the pattern for click-to-select: on mouse.presses(), call world.getSpriteAt(mouse.x, mouse.y) and remember the result. Then you can drag it, change its color, or delete it.","code":"let selected;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  for (let i = 0; i < 6; i++) {\n    let s = new Sprite(60 + i * 55, 200, 40, 40);\n    s.color = 'steelblue';\n  }\n}\n\nfunction update() {\n  if (mouse.presses()) {\n    if (selected) selected.color = 'steelblue';\n    selected = world.getSpriteAt(mouse.x, mouse.y);\n    if (selected) selected.color = 'tomato';\n  }\n}\n\nfunction draw() {\n  background('#222');\n}"}]},{"slug":"joints","title":"Joints","pages":[{"title":"GlueJoint","body":"A GlueJoint welds two sprites together so they move and rotate as a single rigid body. Create it with new GlueJoint(spriteA, spriteB) — just the two sprites, no options object.\n\nOnce glued, changing one sprite's position or velocity carries the other along. Gluing a dynamic sprite to a static one effectively pins it in place.\n\nLike every joint, call joint.delete() to break the connection and let the sprites move independently again.","code":"function setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  new Sprite(200, 380, 400, 20, 'static');\n\n  const stem = new Sprite(200, 200, 20, 100);\n  const cap = new Sprite(200, 150, 100, 20);\n  new GlueJoint(stem, cap);\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"DistanceJoint","body":"A DistanceJoint keeps two sprites at a fixed distance — the basis for ropes, tethers, and pendulum strings. Create it with new DistanceJoint(spriteA, spriteB, options).\n\nSet joint.length after construction to change the target distance. The length is in pixels. You can also pass { length: 100 } in the options object at construction time.\n\nCall joint.delete() to remove the constraint.","code":"let tether;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n\n  const anchor = new Sprite(200, 60, 16, 16, 'static');\n  const ball = new Sprite(320, 60, 30);\n  ball.color = 'tomato';\n\n  tether = new DistanceJoint(anchor, ball);\n  tether.length = 120;\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"HingeJoint","body":"A HingeJoint pins two sprites at a shared point and lets them rotate around it. Use it for pendulums, swinging doors, and anything that needs to pivot.\n\nCreate it with new HingeJoint(spriteA, spriteB, options). Pass { anchor: { x, y } } in the options to set the pivot point in world coordinates — otherwise it defaults to spriteA's position.\n\nCall joint.delete() to remove the hinge.","code":"function setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n\n  const anchor = new Sprite(200, 80, 16, 16, 'static');\n  const rod = new Sprite(200, 180, 16, 180);\n  rod.color = 'khaki';\n\n  new HingeJoint(anchor, rod);\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"SliderJoint","body":"A SliderJoint locks two sprites to a single axis of motion and forbids rotation between them. It's perfect for pistons, elevators, and sliding panels.\n\nCreate it with new SliderJoint(spriteA, spriteB, options). Pass { axis: { x, y } } to set the slide direction — { x: 0, y: 1 } for vertical, { x: 1, y: 0 } for horizontal. Drive the moving sprite with .vel along the axis.\n\nCall joint.delete() to remove the constraint.","code":"let base, piston;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  base = new Sprite(200, 200, 20, 20, 'static');\n  piston = new Sprite(200, 260, 80, 20);\n  piston.color = 'orange';\n\n  new SliderJoint(base, piston, { axis: { x: 0, y: 1 } });\n}\n\nfunction update() {\n  if (kb.pressing('up'))        piston.vel.y = -3;\n  else if (kb.pressing('down')) piston.vel.y = 3;\n  else                          piston.vel.y = 0;\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"WheelJoint","body":"WheelJoint is built for vehicles. Pass the chassis as spriteA and the wheel as spriteB. The wheel can travel along an axis (giving you suspension) and spin freely.\n\nCreate it with new WheelJoint(chassis, wheel, options). Pass { axis: { x, y } } to set the suspension axis — { x: 0, y: 1 } for vertical suspension.\n\nDrive the wheel by setting its angularVelocity each frame. Call joint.delete() to remove the constraint.","code":"let chassis, wheel;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  new Sprite(200, 380, 400, 20, 'static');\n\n  chassis = new Sprite(200, 300, 100, 20);\n  wheel = new Sprite(180, 330, 26);\n  wheel.friction = 1;\n  new WheelJoint(chassis, wheel, { axis: { x: 0, y: 1 } });\n}\n\nfunction update() {\n  if (kb.pressing('right'))     wheel.angularVelocity = 6;\n  else if (kb.pressing('left')) wheel.angularVelocity = -6;\n  else                          wheel.angularVelocity = 0;\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"GrabberJoint","body":"A GrabberJoint lets you pull a physics sprite toward a target position with a spring force. It's how you drag boxes around in a physics puzzle.\n\nCreate it with new GrabberJoint(anchorSprite, draggedSprite, options). The anchor is usually a static sprite at a fixed position. Pass { maxForce: 200 } to control how strong the pull is — higher values feel snappier.\n\nCall joint.delete() to release the sprite.","code":"let box, anchor, grab;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  new Sprite(200, 380, 400, 20, 'static');\n\n  anchor = new Sprite(200, 200, 10, 10, 'static');\n  anchor.visible = false;\n\n  box = new Sprite(200, 300, 50, 50);\n  box.color = 'deepskyblue';\n}\n\nfunction update() {\n  if (mouse.presses() && world.getSpriteAt(mouse.x, mouse.y) === box) {\n    anchor.x = mouse.x;\n    anchor.y = mouse.y;\n    grab = new GrabberJoint(anchor, box, { maxForce: 200 });\n  }\n  if (grab && mouse.pressing()) {\n    anchor.x = mouse.x;\n    anchor.y = mouse.y;\n  }\n  if (mouse.released() && grab) {\n    grab.delete();\n    grab = null;\n  }\n}\n\nfunction draw() {\n  background('#222');\n}"}]},{"slug":"animation","title":"Animation","pages":[{"title":"Procedural animation","body":"Not every animation needs a sprite sheet. The global frameCount increments every frame — combining it with Math.sin, Math.cos, and the modulo operator is enough for bobbing, spinning, pulsing, and color cycling.\n\nMath.sin(frameCount / period) oscillates between -1 and 1. Multiply by an amplitude and add a baseline to shift it into the range you want.\n\nModulo (frameCount % n) cycles through integers 0..n-1 each n frames — great for flashing effects or phase changes.","code":"let orb;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  orb = new Sprite(200, 200, 50);\n  orb.collider = 'none';\n}\n\nfunction update() {\n  orb.x = 200 + 120 * Math.cos(frameCount / 40);\n  orb.y = 200 + 60 * Math.sin(frameCount / 20);\n  orb.rotation = frameCount * 3;\n  orb.scale.x = 1 + 0.25 * Math.sin(frameCount / 15);\n  orb.scale.y = orb.scale.x;\n  orb.color = (frameCount % 60 < 30) ? 'cyan' : 'magenta';\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"addAni and changeAni","body":"moSHion supports minimal sprite-sheet animation. sprite.addAni(name, sheetUrl, frameCount) loads a horizontal frame strip from an image URL and registers it under the given name. The first addAni call auto-activates that animation.\n\nsprite.changeAni(name) switches to a previously registered animation. If the name isn't found, it's a silent no-op — nothing happens.\n\nani.frameDelay controls how many game frames each animation frame is held for (default 4). Set it on the Ani object returned by addAni, or on sprite.ani.frameDelay after activation.\n\nSetting sprite.image clears any active animation, and vice versa — they're mutually exclusive.","code":"let hero;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  hero = new Sprite(200, 200, 64, 64);\n  hero.collider = 'none';\n\n  // In a real project you'd use a URL to your own sprite sheet:\n  // hero.addAni('idle', 'hero-idle.png', 4);\n  // hero.addAni('run', 'hero-run.png', 6);\n\n  // For this sandbox, use an emoji as a stand-in:\n  hero.image = '🧙';\n}\n\nfunction update() {\n  if (kb.pressing('right')) {\n    hero.x += 2;\n    // hero.changeAni('run');\n  } else {\n    // hero.changeAni('idle');\n  }\n}\n\nfunction draw() {\n  background('#222');\n}"}]},{"slug":"text","title":"Text & HUD","pages":[{"title":"Displaying text","body":"text(string, x, y) draws text on the canvas at (x, y). Call it from draw() so it gets re-drawn every frame — otherwise background() will erase it.\n\ntextSize(px) sets the font size in pixels. fill(color) sets the text color (yes, the same fill you use for shapes). textAlign('left' | 'center' | 'right') changes how x positions the string — 'left' puts x at the left edge, 'center' puts x at the middle.\n\nYou can also pass size and color directly to text(): text('hello', 10, 20, 18, 'gold'). The constants CENTER, LEFT, and RIGHT are available for textAlign.\n\nYou can build strings with + just like normal JavaScript: 'Score: ' + score.","code":"let score = 0;\n\nfunction setup() {\n  new Canvas(400, 400);\n}\n\nfunction update() {\n  if (kb.presses(' ')) score++;\n}\n\nfunction draw() {\n  background('#112');\n\n  fill('white');\n  textSize(28);\n  textAlign(CENTER);\n  text('moSHion', 200, 80);\n\n  fill('gold');\n  textSize(18);\n  textAlign(LEFT);\n  text('Score: ' + score, 20, 140);\n\n  fill('#888');\n  textSize(12);\n  text('press space to score', 20, 160);\n}"},{"title":"HUDs that don't scroll","body":"When you move camera.x to follow the player, everything drawn inside draw() scrolls with the world — including your score text. That's rarely what you want for a HUD.\n\nThe simplest approach: draw your HUD at fixed screen coordinates before moving the camera. Since the camera starts at (0, 0) each frame, draw your HUD first, then update the camera position for the rest of the frame.\n\nFor a more robust approach, track the camera offset and subtract it when drawing HUD elements. But for most simple games, drawing the HUD at the top of draw() before any camera movement works fine.","code":"let player;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  new Sprite(1000, 390, 2000, 20, 'static');\n  player = new Sprite(200, 300, 30, 30);\n  player.color = 'deepskyblue';\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.vel.x = -5;\n  else if (kb.pressing('right')) player.vel.x = 5;\n  camera.x += (player.x - camera.x) * 0.1;\n}\n\nfunction draw() {\n  background('#222');\n\n  fill('white');\n  textSize(16);\n  text('x: ' + Math.round(player.x), 10, 24);\n}"},{"title":"Labels on sprites","body":"sprite.text puts a label directly on a sprite — a name tag, an HP counter, a damage number — without you tracking its x/y yourself. It's centered over the sprite and follows it automatically as the sprite moves, and it stays right-side-up even if the sprite rotates. Set it to null (the default) to remove the label.\n\nsprite.textColor and sprite.textSize style it, same idea as fill()/textSize() for the global text() function but scoped to just this one sprite. It respects sprite.opacity too, so a fading sprite's label fades with it.\n\nThis is a different tool from the global text() function above: text() is for one-off draws you position yourself (titles, HUDs); sprite.text is for a label that's tied to a specific sprite.","code":"let enemy;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n\n  enemy = new Sprite(200, 200, 50, 50);\n  enemy.color = 'firebrick';\n  enemy.text = 'HP 3';\n  enemy.textColor = 'white';\n  enemy.textSize = 14;\n}\n\nfunction update() {\n  if (kb.presses(' ')) enemy.text = 'HP 2';\n}\n\nfunction draw() {\n  background('#222');\n}"}]},{"slug":"persistence","title":"Persistence","pages":[{"title":"Saving and loading data","body":"moSHion provides three functions for storing data in the browser's localStorage.\n\nstoreItem(name, value) saves a value under a key. The value is automatically JSON-stringified, so you can store numbers, strings, arrays, and objects.\n\ngetItem(name) retrieves a previously stored value, parsing it back from JSON. Returns null if the key doesn't exist.\n\nremoveItem(name) deletes a stored value.\n\nThis is how you save high scores, level progress, or player preferences between sessions. Data persists even after the browser is closed.","code":"let highScore = 0;\nlet score = 0;\n\nfunction setup() {\n  new Canvas(400, 400);\n  highScore = getItem('highScore') || 0;\n}\n\nfunction update() {\n  if (kb.presses(' ')) {\n    score++;\n    if (score > highScore) {\n      highScore = score;\n      storeItem('highScore', highScore);\n    }\n  }\n  if (kb.presses('r')) {\n    score = 0;\n    removeItem('highScore');\n    highScore = 0;\n  }\n}\n\nfunction draw() {\n  background('#222');\n  fill('white');\n  textSize(18);\n  text('Score: ' + score, 14, 30);\n  fill('gold');\n  text('High: ' + highScore, 14, 54);\n  fill('#888');\n  textSize(12);\n  text('space=score  r=reset', 14, 76);\n}"}]},{"slug":"patterns","title":"Game Patterns","pages":[{"title":"Top-down movement","body":"For top-down games, set world.gravity.y = 0 so nothing falls. Move the player by writing to vel.x and vel.y based on the arrow keys.\n\nClamp the player's position to the canvas edges each frame so it can't leave the screen. A simple Math.max / Math.min pair handles this.\n\nSetting vel to 0 when no key is pressed gives a snappy, arcade feel. For drifty movement, leave vel alone and add small amounts instead.","code":"let player;\nconst SPEED = 4;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  player = new Sprite(200, 200, 30, 30);\n  player.color = 'deepskyblue';\n}\n\nfunction update() {\n  player.vel.x = 0;\n  player.vel.y = 0;\n  if (kb.pressing('left'))  player.vel.x = -SPEED;\n  if (kb.pressing('right')) player.vel.x = SPEED;\n  if (kb.pressing('up'))    player.vel.y = -SPEED;\n  if (kb.pressing('down'))  player.vel.y = SPEED;\n\n  player.x = Math.max(15, Math.min(385, player.x));\n  player.y = Math.max(15, Math.min(385, player.y));\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Platformer jump","body":"A platformer needs gravity, a ground-check, and edge-triggered jump input. Keep the ground sprite in a variable so you can test player.colliding(ground) before jumping.\n\nUse kb.pressing for continuous left/right motion, and kb.presses(' ') for the jump so one tap equals one jump.\n\nFor coyote time or double jumps, count frames since the last ground contact and allow jumps while that count is small.","code":"let player, ground;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 10;\n  player = new Sprite(200, 100, 30, 40);\n  player.color = 'deepskyblue';\n  ground = new Sprite(200, 380, 400, 20, 'static');\n  new Sprite(120, 300, 80, 12, 'static');\n  new Sprite(300, 240, 80, 12, 'static');\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.vel.x = -5;\n  else if (kb.pressing('right')) player.vel.x = 5;\n\n  if (kb.presses(' ') && player.colliding(ground)) {\n    player.vel.y = -9;\n  }\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Projectiles from a player","body":"Keep bullets in a Group so they share defaults and are easy to iterate. Setting group defaults (color, diameter, collider) once saves repeating yourself each spawn.\n\nSpawn a new bullet with new bullets.Sprite(x, y) when the fire key is pressed. Give it a velocity so it flies across the screen.\n\nRemove bullets once they leave the canvas — otherwise they pile up and slow the sketch down. Iterate a copy with [...bullets] so removing doesn't break the loop.","code":"let player, bullets;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  player = new Sprite(60, 200, 30, 30);\n  player.color = 'deepskyblue';\n\n  bullets = new Group();\n  bullets.diameter = 10;\n  bullets.color = 'gold';\n  bullets.collider = 'none';\n}\n\nfunction update() {\n  if (kb.pressing('up'))        player.vel.y = -4;\n  else if (kb.pressing('down')) player.vel.y = 4;\n  else                          player.vel.y = 0;\n\n  if (kb.presses(' ')) {\n    const b = new bullets.Sprite(player.x + 20, player.y);\n    b.vel.x = 8;\n  }\n\n  for (const b of [...bullets]) {\n    if (b.x > 420) b.delete();\n  }\n}\n\nfunction draw() {\n  background('#222');\n}"},{"title":"Score and timer HUD","body":"Use plain variables to track game state: one for score, one for the remaining time. Increase the score when the player overlaps a collectible, and remove the collectible so it doesn't re-trigger.\n\nConvert frameCount into seconds by dividing by 60 (the default frame rate). Math.max(0, ...) stops the timer clamping into negative numbers.\n\nDraw the HUD in draw() after background() so it sits on top. Use fixed x/y coordinates so the HUD stays put if the camera scrolls.","code":"let player, coins;\nlet score = 0;\nconst LIMIT = 20;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  player = new Sprite(200, 200, 30, 30);\n  player.color = 'deepskyblue';\n\n  coins = new Group();\n  coins.diameter = 18;\n  coins.color = 'gold';\n  coins.collider = 'none';\n  for (let i = 0; i < 6; i++) {\n    new coins.Sprite(40 + i * 60, 80 + (i % 2) * 200);\n  }\n}\n\nfunction update() {\n  if (kb.pressing('left'))       player.vel.x = -4;\n  else if (kb.pressing('right')) player.vel.x = 4;\n  else                           player.vel.x = 0;\n  if (kb.pressing('up'))         player.vel.y = -4;\n  else if (kb.pressing('down'))  player.vel.y = 4;\n  else                           player.vel.y = 0;\n\n  for (const c of [...coins]) {\n    if (player.overlapping(c)) {\n      score++;\n      c.delete();\n    }\n  }\n}\n\nfunction draw() {\n  background('#222');\n  const timeLeft = Math.max(0, LIMIT - Math.floor(frameCount / 60));\n  fill('white');\n  textSize(18);\n  text('Score: ' + score, 14, 26);\n  text('Time: ' + timeLeft, 14, 50);\n}"},{"title":"Scene/state switching","body":"A single state variable is enough for most small games. Use strings like 'menu', 'play', and 'gameover' — they're easy to read in if/else branches.\n\nIn update(), branch on state to handle input for that scene only. In draw(), branch on state to render the right screen. Changing state is just state = 'play'.\n\nKeep scene-specific sprites hidden or disabled when not in that scene. The simplest approach is to only create them when entering 'play' and remove them when leaving.","code":"let state = 'menu';\nlet player;\n\nfunction setup() {\n  new Canvas(400, 400);\n  world.gravity.y = 0;\n  player = new Sprite(200, 200, 30, 30);\n  player.color = 'deepskyblue';\n  player.visible = false;\n}\n\nfunction update() {\n  if (state === 'menu' && kb.presses(' ')) {\n    state = 'play';\n    player.visible = true;\n  } else if (state === 'play') {\n    if (kb.pressing('left'))       player.vel.x = -4;\n    else if (kb.pressing('right')) player.vel.x = 4;\n    else                           player.vel.x = 0;\n    if (kb.presses('x')) state = 'gameover';\n  } else if (state === 'gameover' && kb.presses(' ')) {\n    state = 'menu';\n    player.visible = false;\n  }\n}\n\nfunction draw() {\n  background('#222');\n  fill('white');\n  textSize(20);\n  if (state === 'menu') {\n    text('Press SPACE to start', 90, 200);\n  } else if (state === 'play') {\n    text('Press X to end', 14, 24);\n  } else {\n    text('Game Over', 140, 180);\n    text('SPACE for menu', 115, 220);\n  }\n}"}]}];
